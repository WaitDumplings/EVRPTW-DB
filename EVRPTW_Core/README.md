# EVRPTW_Core

Shared schema, instance loading, solution validation, and metric computation will live here.

This layer is intentionally separated so both the dataset generator and benchmark solvers use the same data contract.
