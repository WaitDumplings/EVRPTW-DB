run_name: TERRAN
dataset_name: Amazon_Calibrated_v1

data:
  generator_config: configs/amazon_hierarchy.yaml
  num_customers: 100
  num_charging_stations: 20
  mother_board_pool_size: 8
  mother_num_customers: 5000
  mother_num_charging_stations: 120
  region_reuse_limit: 200
  max_attempts_per_instance: 30
  async_instance_prefetch: true
  async_instance_workers: 8
  async_instance_queue_batches: 2
  async_multiprocessing_context: spawn

env:
  use_fast_env: true
  use_jit_mask: true
  normalize_reward: true
  reward_distance_scale_mode: single_customer_repair_sum
  charging_mode: fixed_full

model:
  embedding_dim: 256
  tanh_clipping: 15.0
  n_encode_layers: 3
  use_graph_token: false
  use_dynamic_embedding: false

training:
  online_training: true
  epochs: 300
  num_envs_per_gpu: 64
  n_traj: 50
  rollout_steps: 140
  ppo_update_epochs: 3
  num_minibatches: 16
  gamma: 0.99
  clip_coef: 0.2
  vf_coef: 0.5
  ent_coef: 0.01
  learning_rate: 0.0001
  weight_decay: 0.0
  max_grad_norm: 1.0
  checkpoint_interval: 50
  debug: false
  debug_log_every: 1
  profile_timing: false

pbrs:
  use_customer_pbrs: false
  use_repair_distance_pbrs: false
  use_feasible_ratio_pbrs: false
  use_terminal_heuristic: false

evaluation:
  eval_interval: 10
  eval_path: EVRPTW_Dataset/Amazon_Calibrated_v1/Cus_100/CS_20/eval_100
  eval_n_traj: 50
  eval_decode_mode: sample
  eval_max_steps: 180
  eval_limit: null
