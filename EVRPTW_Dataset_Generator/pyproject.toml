[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "evrptw-cle"
version = "0.3.0"
description = "Reproducible City Logistics Environments for practical EVRPTW benchmarks"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "geopandas>=1.0,<2",
  "folium>=0.20,<1",
  "matplotlib>=3.8,<4",
  "networkx>=3.2,<4",
  "numpy>=1.26,<3",
  "osmnx>=2.0,<3",
  "pandas>=2.1,<3",
  "pillow>=10,<13",
  "pyarrow>=15,<22",
  "pyogrio>=0.9,<1",
  "requests>=2.31,<3",
  "scipy>=1.12,<2",
  "shapely>=2.0,<3",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "ruff>=0.9,<1",
]

[project.scripts]
evrptw-cle = "evrptw_cle.cli:main"
evrptw-stage2 = "evrptw_stage2.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/evrptw_cle", "src/evrptw_stage2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Freeze Ruff's historical correctness-oriented default. New Ruff releases
# must not silently enable broad style/docstring rule families in CI.
select = ["E4", "E7", "E9", "F"]
